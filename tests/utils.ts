import type { ConnInfo } from "../src/types.ts";

export const connInfo: ConnInfo = {
  localAddr: {
    hostname: "",
    port: 80,
    transport: "tcp",
  },
  remoteAddr: {
    hostname: "",
    port: 8080,
    transport: "tcp",
  },
};
